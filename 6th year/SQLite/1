import sqlite3

name = input("Enter a name: ")
age = int(input("Enter a number:"))
#Connect to a database
#This will create an empty file on your computer with the name of the database
#if the file does not already exist
db_connection = sqlite3.connect("mydatabase.db")
#create a cursor object
cursor = db_connection.cursor()
#create a docstring which contains the SQL command for the cursor to run
createTable = '''create table if not exists users(
id integer primary key autoincrement,
name text not null,
age integer
) STRICT'''
#Execute the SQL command
#This will create an empty table in our database
cursor.execute(createTable) #run the SQL command
db_connection.commit() #save the change to the database
#create a docstring which contains the SQL command for the cursor to run
addData = '''insert into users (name,age) values (?,?)'''
cursor.execute(addData,(name,age))
#save the changes to the database
db_connection.commit()
#create a docstring which contains the SQL command for the cursor to run
fetchData = '''select * from users'''
cursor.execute(fetchData)
#cursor.fetchall() retrieves all the remaining #rows of the table. If there are no
#rows it #returns an empty list
rowsInTable = cursor.fetchall()
for row in rowsInTable:
    print(row)
# Using a while loop
# fetchData = '''select * from users'''
# cursor.execute(fetchData)
# row = cursor.fetchone()
# while row is not None:
#     print(row)
cursor.close()
db_connection.close()
# #row = cursor.fetchone()
# #fetchData = '''select * from users'''
# #cursor.execute(fetchData)
# while True:
#     result = cursor.fetchmany(3)#3 is just a random number of rows
#     if not result:
#         print('All results read in')
#         break
#     else:
#         print(result)
# # Using the cursor as iterator
# fetchData = '''select * from users'''
# cursor.execute(fetchData)
# for row in cursor:
#     print(row)
# # Using the cursor as iterator
# fetchData = '''select name,age from users'''
# cursor.execute(fetchData)
# for row in cursor:
#     print(row)
# cursor.close()
# db_connection.close()
# y = open('some_data.csv','r')
# y.readline() #read in the header line which has no data for the table
# for line in y:
#     line = line.strip() # remove the newline character
# db = tuple(line.split(',')) #create a tuple for the database
# #create a docstring which contains the SQL command for the cursor to run
# cursor.execute(addData,db)
# #save the changes to the database
# db_connection.commit()
