def mean_numbs(a):
    if len(e)%2==1:
        result = e[(int((a+1)/2)-1)]
    else:
        result = (e[int((a+1)/2)]+e[int((a+1)/2)-1])/2
    return result
e = eval(input("enter numbs: "))
x = mean_numbs(len(e))
print(x)
