def often_numb(a):
    result = a
    for i in range(0,len(e)):
        if r == e[i]:
            result += 1
    return result
e = eval(input("enter numbs: "))
r = int(input("Enter a number to find the frequency of occurence: "))
x = often_numb(0)
print(f"The number {r} occurs {x} times")
